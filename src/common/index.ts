export namespace Payload {
  export interface Tournament {
    endDate: string,
    format: string,
    id: string,
    logoUrl: string,
    name: string,
    startDate: string,
    tier: string,
    twitchUrl: string
  }
}